<%= form_for(@micropost) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
    <%= f.text_area :content, placeholder: "Введите текст" %>
    </div>
    <%= f.submit "Запостить", class: "btn btn-primary" %>
<% end %>